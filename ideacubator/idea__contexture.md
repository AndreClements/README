`ideacubator\idea__contexture.md`
_Status:`developing`_
**Origin:** conversation, 2026-04-03
**Related:** `ideacubator/idea__artificial_vs_human_dissolves.md`, `ideacubator/idea__stocks_and_flows.md`, `docs/methods/METHODOLOGY_CII.md`, `projects/theses/parametric_authorship.md` (A6 Provenance is the Indexical Trace)
**Empirical anchor:** ArtPraxis `studio/life-drawing/20260403-andrea/sketchbook-sequence.md` §1–§2 — six sketchbook spreads trace the development of a compression/decompression diptych. The raised arm hunted across spread 0870, isolated in 0868, committed to in the final diptych: each page carries the compressed residue of prior pages. The contexture of the sequence is visible — the trace is haptic (charcoal density, gestural confidence) even when the specific ancestry (which pose, which minute) is lost.

---

## Core Provocation

**Contexture**: That surface feel that remains from the temporal compression of rolling contexts.

## Notes

The neologism fuses *context* and *texture*. In rolling-context systems (agentic conversations, compressed message histories, palimpsest documents), prior context doesn't vanish — it leaves a residue, a felt quality in the surface of what follows. The texture of a paragraph shaped by compressed prior exchange is different from one written fresh. The provenance is felt even when the specific ancestry is lost.

This connects to PA's A6 (Provenance is the Indexical Trace): `indexical_trace := ledger(commit_hashes, locks, deltas)`. Contexture is what happens when the ledger is compressed — the trace becomes haptic rather than auditable. The hash is lost but the grain remains.

Every lived life has its contexture, also.

---
