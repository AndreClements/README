# README — Human-OS

A cyber artist's book by Andre S Clements. Content, frameworks, art.

**Spelling:** South African English with UK bias (organisation, recognised, colour, favour, analyse, defence, practise/practice).

---

## Methodology

Governance lives in `docs/methods/METHODOLOGY_CII.md`. Start at **§0 Dispatch Table** to load only the sections your tier requires:

| Task type | Read §… |
|-----------|---------|
| T1 (Skirmish / exploration) | §0, §1, §3.1, §7 |
| T2 (Project / external output) | §0–§5, §7–§8 |
| T3 (Publication / exhibition) | All sections |

**Evaluation order:** Axiology-first (CARDS: Competence, Autonomy, Relatedness, Dignity, Safety) → Poetry/artistry → Ethos/technicality. Values first; craft last.

**Host ≠ Author** principle applies to all lib entries.

**Voice (prose-producing sessions):** When the session will generate prose for the repo (lib entry, lens, essay, methodology doc), load [docs/methods/voice_signature.md](docs/methods/voice_signature.md) alongside `METHODOLOGY_CII.md`. It carries the operator's voice contract — registers, signature moves, operational vocabulary, anti-patterns — and is the reference for LLM-hygiene checks. Not required for code-only or research-brief sessions.

---

## META Integration (Operational Learning)

Process-level observations about the methodology live in the **PRIVATE_META repo** (`../PRIVATE_META/`), not in this repo. See `METHODOLOGY_CII.md §8.5`.

> `../PRIVATE_META/` assumes sibling repos under the same parent directory. Adjust if layout changes.

### Session open

Read `../PRIVATE_META/context/context__readme_repo.md` to orient — it holds this project's architecture, conventions, and active work streams.

### Session close

After completing primary work, check for three things:

1. **Process observations?** If you noticed something about the methodology or protocol itself, append a field note to `../PRIVATE_META/field_notes/field_notes__[document].md`. Schema: `../PRIVATE_META/PROTOCOL__field_notes.md §1`.

2. **Diagnostic run?** If a self-diagnostic was run (§6), append a one-line summary to `../PRIVATE_META/diagnostic_log.md`. Format: `../PRIVATE_META/PROTOCOL__field_notes.md §2`.

3. **Context changed?** If project context changed materially, update `../PRIVATE_META/context/context__readme_repo.md`.

Content-level reflections stay in this repo's `staging/reflections/`, not META.
